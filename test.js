var pokemonList = objeto;
var pokeNames = [];

for (pokemon of pokemonList) {
    pokeNames.push(results.name)
};


// var mapResults = forms.map((pokemon) => results.nome)
//     // var mapResults = pokemonDataList.map(getPokemonName)
// console.log("resultado do map", mapResults)


//const nomespokemon = data1.filter(forms => (nome.pokemonAleatorio));

//console.log(pokemonleatorio);

//const pokemonAleatorio = data.map(empresa => `${empresa.nome}` + ' CEO: ' + `${empresa.CEO}`);
//console.log(exibeInformacoes)